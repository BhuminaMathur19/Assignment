import {width, margin}  from '@material-ui/core';
import NotFound from '../Assets/Images/NotFound.png';

const Bar = () => {
    return (
        <img src={NotFound} style={{width: '30%', margin:'80% 0 0 35%'}}/>
    )
}

export default Bar ;
